#include <iostream>
#include <stack>
#include <utility>
using namespace std;

int main(void)
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int n = 1;
    while (n)
    {
        cin >> n;
        for (int i = 0; i < n; i++)
        {
            int m;
            cin >> m;
        }
    }

    return 0;
}